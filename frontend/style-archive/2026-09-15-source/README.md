# Pre-rebuild style source snapshot

This directory preserves the runtime CSS source files before the style-system
rebuild. Paths below this directory mirror `frontend/app` and `frontend/public`.

These files are reference-only. Runtime imports continue to use their original
locations until the replacement stylesheet for each surface is ready.
